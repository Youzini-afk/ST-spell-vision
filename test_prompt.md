# Spell Vision 测试提示词

用于在 SillyTavern 中快速回归 `spell-vision` 扩展。  
建议先确保世界书已启用，模型会输出 `<spell>...</spell>`。

## 基础功能

1. 火球术（单法术）
```text
你是奇幻小说旁白。描写我释放一个火球术，命中前方木人。要求有动作细节，并输出一个 <spell> 标签描述法术视觉效果。
```

2. 冰霜护盾（单法术）
```text
描写角色在危急时刻展开冰霜护盾，挡住箭雨。务必输出一个 <spell> 标签。
```

3. 治疗术（单法术）
```text
描写牧师施放治疗术，光芒从手心扩散并修复伤口。请输出一个 <spell> 标签。
```

4. 黑暗系法术（单法术）
```text
描写术士施放黑暗侵蚀法术，环境变冷、阴影扭曲。请输出一个 <spell> 标签。
```

## 多法术与复杂场景

5. 一条消息两个法术
```text
写一段战斗：先施放加速术，再施放雷击术。每个法术都单独输出 <spell> 标签。
```

6. 连续三法术（压力）
```text
描写法师进行连携施法：风刃、火墙、陨星坠落。每个法术后都给出一个 <spell> 标签。
```

7. 超大场景（复杂元素）
```text
描写一个大型禁咒，覆盖整片天空，包含符文阵、旋转光环、粒子雨。输出一个详细 <spell> 标签。
```

## 语言与鲁棒性

8. 中文叙事 + 英文 spell 描述
```text
用中文写剧情，但 <spell> 标签内部必须用英文描述视觉效果。施放“奥术洪流”。
```

9. 英文叙事
```text
Narrate a high-fantasy spell cast in English. Include exactly one <spell> tag with vivid visual details.
```

10. 混合标点与换行
```text
写一段带换行的施法场景，<spell> 标签内部至少 3 句描述，包含颜色、几何形状、运动轨迹。
```

## 边界与异常测试

11. 空 spell（应不渲染或报错可控）
```text
输出一段文字，并包含空标签：<spell>   </spell>
```

12. 非法标签大小写（解析容错）
```text
输出一个法术描述，但标签写成 <SPELL>...</SPELL>。
```

13. 缺失闭合标签（异常输入）
```text
输出施法文本，只给 <spell> 开始标签，不要结束标签。
```

14. 标签中夹杂代码块
```text
输出施法文本，<spell> 内加入类似 JSON 的描述和反引号代码块标记。
```

## 缓存与重复渲染验证

15. 相同 spell 描述重复发送（缓存）
```text
请连续两次输出完全相同的 <spell> 描述（文字一字不差），用于测试缓存命中。
```

16. 同一条消息重复打开聊天（去重）
```text
输出一个正常法术 <spell>。然后我会切换聊天再切回来，检查是否重复渲染。
```

## 可视化效果覆盖

17. 几何图元覆盖
```text
施放“几何构型术”，在 <spell> 里强调圆形、线段、多边形、路径光迹、文本符文。
```

18. 动画类型覆盖
```text
施放“星界脉动”，在 <spell> 里强调脉冲、旋转、淡入、漂浮、闪烁等动态效果。
```

19. 粒子效果覆盖
```text
施放“流星尘暴”，在 <spell> 中强调大量漂浮粒子、不同速度、辉光尾迹。
```

20. 极简法术（最小有效）
```text
只写一个非常简短的法术视觉描述（1 句）放进 <spell> 标签。
```

## 建议执行顺序

先跑：`1 -> 5 -> 8 -> 15 -> 16 -> 11 -> 13 -> 17 -> 18 -> 19`

